# OpenAI API key. Never commit the real key to source control.
OPENAI_API_KEY=

# Responses API model
OPENAI_MODEL=gpt-4o-mini

# Set to 1 only when the model should use web search.
SEARCH_WEB=0

# Local server port
PORT=5500
